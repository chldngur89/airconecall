# When your API is ready, set the base URL and wire `submitRequest` in src/app/App.tsx
# Vercel: Project → Settings → Environment Variables
VITE_API_BASE_URL=

# 공개 사이트 URL(끝 슬래시 없이). canonical·OG·structured data·robots/sitemap 에 주입합니다.
VITE_SEO_ORIGIN=
